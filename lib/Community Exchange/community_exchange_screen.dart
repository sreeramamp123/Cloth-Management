import 'package:flutter/material.dart';

class CommunityExchangeScreen extends StatelessWidget {
  const CommunityExchangeScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text(
        'Community Exchange Screen',
        style: TextStyle(fontSize: 10),
      ),
    );
  }
}
